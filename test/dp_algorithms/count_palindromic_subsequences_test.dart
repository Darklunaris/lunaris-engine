import 'package:test/test.dart';
import 'package:lunaris_engine/lunaris_engine.dart';

void main() {
  group('countPalindromicSubsequences', () {
    test('aaa', () {
      expect(countPalindromicSubsequences('aaa'), equals(6));
    });
  });
}
